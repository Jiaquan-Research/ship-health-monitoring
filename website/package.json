{
  "name": "jqhe-public-research-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "npm run sync-manifests && node scripts/validate-publication.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "dev": "astro dev",
    "preview": "astro preview",
    "sync-manifests": "node scripts/sync-manifests.mjs"
  },
  "dependencies": {
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
